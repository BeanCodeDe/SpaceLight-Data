CREATE TABLE spacelight_data.ship_type (
    id uuid PRIMARY KEY NOT NULL,
    name varchar NOT NULL
    rome_place_list json NOT NULL
    door_list json NOT NULL
    weapon_place_list json NOT NULL
);